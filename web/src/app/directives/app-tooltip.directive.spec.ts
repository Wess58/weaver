import { AppTooltipDirective } from './app-tooltip.directive';

describe('AppTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
