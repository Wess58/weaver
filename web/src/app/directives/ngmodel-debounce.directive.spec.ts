import { NgmodelDebounceDirective } from './ngmodel-debounce.directive';

describe('NgmodelDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new NgmodelDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
