import { FigureFormatterPipe } from './figure-formartter.pipe';

describe('FigureFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new FigureFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
